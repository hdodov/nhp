var a = require('test');

if (/test/.exec("str\"ing")) { // "com'ment'"
    <div ${"foo" + console.log()}> <!-- begin div -->
        <echo plain>
            hey, "this" is `some cool data`
        </echo> //fsdf
    </div>

    <div></div>
    <div></div>

    /* no longer needed
    <div>test</div>
    */

    for (var i = Things.length - 1; i >= 0; i--) {
        <li data-test="${i}">${ Things[i] }</li>
    }

    <echo
        plain
        trim
        escape
    >
        foobar
    </echo>

    <echo test="${if (bar) "test"}">
        <h1>ready</h1>
    </echo>
}
