var ms = require('ms');
var foo = require('./foo');

<!DOCTYPE html>
<html>
<head>
    <title>NHP</title>
</head>
<body>
    <div>
        // var prom = new Promise((resolve, reject) => {
        //     setTimeout(function () {
        //         resolve('working');
        //     }, 500);
        // });

        <h1>stara</h1>

        var cnt = capture(() => {
            <h1>${ arguments.query.test }</h1>
        });

        <pre>
            <template>
                ${ cnt.toUpperCase() }
            </template>
        </pre>


        await include('./mods/md.nhp', {
            wait: 150
        }).then((module) => {
            <h1>${ module.buffer }</h1>
        }).catch(() => {
            <h1>Could not load md.nhp</h1>
        });

        <h1>${ ms(Math.random() * 10e4) }</h1>
        <h2>${ arguments.foo }</h2>

        <div>
            <template>
                foo
                bar
                baz
            </template>
        </div>

        <ul>
            for (var i = 0; i < 50; i++) {
                <li data-index="${i}">${i}</li>
            }
        </ul>

        exports.test = Date.now();

        <template>
            for (var i = 0; i < 50; i++) {
                console.log('5');
            }
        </template>
    </div>
</body>
</html>
