<div>
    var prom = new Promise((resolve, reject) => {
        setTimeout(function () {
            resolve('working');
        }, 500);
    });

    <h1>${ await prom }</h1>

    <ul>
        for (var i = 0; i < 50; i++) {
            <li>${i}</li>
        }
    </ul>

    <template>
        foo bar
        ${ 5 + 23 }
        baz
    </template>
</div>
